# مشروع مينو خان الجمر الإلكتروني

هذه نسخة المصدر الكاملة والقابلة للتعديل خارج ChatGPT. يمكنك فتح المجلد في **VS Code** وتعديل واجهة الزبون، لوحة المدير، الأصناف، الصور، الأسعار، العروض والشكاوى.

## المتطلبات

- Node.js إصدار 22.13 أو أحدث
- VS Code
- قاعدة بيانات PostgreSQL (محلياً عبر Docker، أو أي قاعدة Postgres تصلها عبر الإنترنت)
- اتصال بالإنترنت عند تثبيت المكتبات أول مرة

## التشغيل على الكمبيوتر

1. فك ضغط الملف.
2. افتح المجلد داخل VS Code عبر **File > Open Folder**.
3. افتح Terminal داخل VS Code.
4. نفّذ:

```bash
npm install
```

5. انسخ الملف `.env.example` وسمِّ النسخة الجديدة `.env`.
6. غيّر رمز المدير (`ADMIN_PASSWORD`) والقيمة السرية (`ADMIN_SESSION_SECRET`)، وحدّد `DATABASE_URL` لقاعدة بيانات Postgres عندك (مثال محلي عبر Docker: `docker run -d -e POSTGRES_PASSWORD=devpass -e POSTGRES_DB=khan -p 5432:5432 postgres:16-alpine`).
7. طبّق جداول قاعدة البيانات أول مرة:

```bash
npm run db:migrate
```

8. شغّل المشروع:

```bash
npm run dev
```

9. افتح الرابط الذي يظهر في Terminal. صفحة المدير موجودة على المسار `/admin`.

## أهم أماكن التعديل

- `app/page.tsx`: واجهة المينو للزبائن.
- `app/admin/page.tsx`: لوحة المدير.
- `app/globals.css`: الألوان، الخطوط والتصميم.
- `lib/menu-data.ts`: الوجبات الأساسية والأسعار والأقسام.
- `public/menu/`: الشعار وصور الوجبات الأساسية.
- `app/api/`: عمليات الحفظ، الشكاوى، الصور والعروض.
- `db/schema.ts`: جداول قاعدة البيانات.

## الحفظ وقاعدة البيانات

المشروع يستخدم:

- قاعدة بيانات **PostgreSQL** لحفظ الوجبات والأسعار والعروض والشكاوى (عبر متغير `DATABASE_URL`).
- تخزين ملفات محلي على القرص لحفظ صور الوجبات والعروض (مجلد يحدده متغير `UPLOADS_DIR`، الافتراضي `./uploads`).

عند النشر على استضافة مثل Railway، تأكد من ربط **Volume** بنفس مسار `UPLOADS_DIR` حتى لا تُفقد الصور المرفوعة عند كل نشر جديد (بدون Volume، يُعاد إنشاء القرص فارغاً في كل عملية نشر).

عند تشغيله محلياً، ينشئ نظام التطوير بيانات محلية منفصلة عن بيانات الموقع المنشور.

## النشر على Railway

1. أنشئ مشروعاً جديداً على [railway.app](https://railway.app) واربطه بمستودع الكود (GitHub) أو ارفعه عبر Railway CLI.
2. أضف خدمة **PostgreSQL** من متجر Railway؛ ستحصل تلقائياً على متغير `DATABASE_URL` يمكنك ربطه بخدمة التطبيق.
3. أضف **Volume** للتطبيق واربطه بمسار (مثلاً `/data/uploads`)، ثم اضبط `UPLOADS_DIR=/data/uploads`.
4. أضف باقي المتغيرات في تبويب Variables: `ADMIN_PASSWORD`, `ADMIN_SESSION_SECRET`.
5. Railway يكتشف تلقائياً أوامر `npm run build` و`npm start` من `package.json` — لا حاجة لإعداد إضافي. أمر `npm start` يطبّق جداول قاعدة البيانات تلقائياً (`drizzle-kit migrate`) قبل تشغيل السيرفر، لذا لا تحتاج خطوة يدوية بعد أول نشر.

## إنشاء نسخة جاهزة للنشر محلياً

```bash
npm run build
```

بعد نجاح الأمر ستظهر ملفات النسخة الجاهزة داخل مجلد `dist`.

## ملاحظات مهمة

- لا ترفع ملف `.env` إلى GitHub لأنه يحتوي رمز المدير ورابط قاعدة البيانات (الملف مستثنى تلقائياً عبر `.gitignore`).
- غيّر `ADMIN_SESSION_SECRET` إلى نص عشوائي طويل، وغيّر `ADMIN_PASSWORD` عن القيمة التجريبية `123456` قبل أي نشر حقيقي.
- الصور المرفوعة من لوحة المدير تُحفظ على القرص (مجلد `UPLOADS_DIR`)، وليست داخل سورس المشروع نفسه — تأكد من ربط Volume عند النشر.
- لديك كامل الصلاحية لتعديل هذه الملفات، نسخها، وتسليمها إلى أي مبرمج للعمل عليها.
