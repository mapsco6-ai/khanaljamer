# مشروع مينو خان الجمر الإلكتروني

هذه نسخة المصدر الكاملة والقابلة للتعديل خارج ChatGPT. يمكنك فتح المجلد في **VS Code** وتعديل واجهة الزبون، لوحة المدير، الأصناف، الصور، الأسعار، العروض والشكاوى.

## المتطلبات

- Node.js إصدار 22.13 أو أحدث
- VS Code
- اتصال بالإنترنت عند تثبيت المكتبات أول مرة

## التشغيل على الكمبيوتر

1. فك ضغط الملف.
2. افتح المجلد داخل VS Code عبر **File > Open Folder**.
3. افتح Terminal داخل VS Code.
4. نفّذ:

```bash
npm install
```

5. انسخ الملف `.dev.vars.example` وسمِّ النسخة الجديدة `.dev.vars`.
6. غيّر رمز المدير والقيمة السرية داخل `.dev.vars`.
7. شغّل المشروع:

```bash
npm run dev
```

8. افتح الرابط الذي يظهر في Terminal. صفحة المدير موجودة على المسار `/admin`.

## أهم أماكن التعديل

- `app/page.tsx`: واجهة المينو للزبائن.
- `app/admin/page.tsx`: لوحة المدير.
- `app/globals.css`: الألوان، الخطوط والتصميم.
- `lib/menu-data.ts`: الوجبات الأساسية والأسعار والأقسام.
- `public/menu/`: الشعار وصور الوجبات الأساسية.
- `app/api/`: عمليات الحفظ، الشكاوى، الصور والعروض.
- `db/schema.ts`: جداول قاعدة البيانات.

## الحفظ وقاعدة البيانات

المشروع المنشور يستخدم:

- قاعدة بيانات D1 لحفظ الوجبات والأسعار والعروض والشكاوى.
- تخزين R2 لحفظ صور الوجبات والعروض.

عند تشغيله محلياً، ينشئ نظام التطوير بيانات محلية منفصلة عن بيانات الموقع المنشور. تعديل ملفات الكود لا يمنح أي شخص رمز دخول المدير المنشور، لأن الرمز الحقيقي محفوظ في إعدادات الاستضافة وليس داخل ملفات المشروع.

## إنشاء نسخة جاهزة للنشر

```bash
npm run build
```

بعد نجاح الأمر ستظهر ملفات النسخة الجاهزة داخل مجلد `dist`.

## ملاحظات مهمة

- لا ترفع ملف `.dev.vars` إلى GitHub لأنه يحتوي رمز المدير.
- غيّر `ADMIN_SESSION_SECRET` إلى نص عشوائي طويل.
- الصور المرفوعة من لوحة المدير في الموقع المنشور ليست مخزنة داخل سورس المشروع؛ هي محفوظة في مساحة التخزين المرتبطة بالموقع.
- لديك كامل الصلاحية لتعديل هذه الملفات، نسخها، وتسليمها إلى أي مبرمج للعمل عليها.
